[project]
name = "basic-model"
version = "0.1.0"
dependencies = [
    "sqlalchemy>=2.0.49",
    "basic",
]

[tool.uv.workspace]
members = [
    "src/basic",
]

[tool.uv.sources]
basic = { workspace = true }